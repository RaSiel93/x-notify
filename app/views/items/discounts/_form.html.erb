<%= form_for [:items, @discount] do |f| %>
  <% if @discount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discount.errors.count, "error") %> prohibited this discount from being saved:</h2>

      <ul>
      <% @discount.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :item do |f_item| %>
    <p>
      <%= f_item.label :title %><br/>
      <%= f_item.text_field :title %>
    </p>
    <p>
      <%= f_item.check_box :published %>
      <%= f_item.label :published %>
    </p>
  <% end %>
 
  <p>
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </p>

  <p>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </p>
 
  <p><%= f.submit "Create" %></p>

<% end %>